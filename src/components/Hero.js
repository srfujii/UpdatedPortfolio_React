import React from "react";
// import "./style.css";

function Hero(props) {
    return (
        <div class="hero">
            <h2>while (!(succeed = try()));</h2>
        </div>
    );
}

export default Hero;
